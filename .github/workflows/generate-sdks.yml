name: generate-sdks

on:
  push:
    paths:
      - "src/objects/api/v*/openapi.yaml"
      - ".github/workflows/generate-sdks.yml"
    branches:
      - '**'
  workflow_dispatch:

jobs:
  open-api-workflow-generate-sdks:
    uses: maykinmedia/open-api-workflows/.github/workflows/generate-sdks.yml@0a5525c13f8eea10be40c8eaee5e535ea3a10721 # v6.4.1
    strategy:
      matrix:
        version:
          - v2
          - v1
    with:
      node-version: '18'
      schema-path: 'src/objects/api/${{ matrix.version }}/openapi.yaml'
