name: check-oas

on:
  push:
    paths:
      - "src/objects/api/*/openapi.yaml"
      - .github/workflows/oas-check.yml
    branches:
      - '**'
  workflow_dispatch:

jobs:
  open-api-workflow-check-oas:
    uses: maykinmedia/open-api-workflows/.github/workflows/oas-check.yml@0a5525c13f8eea10be40c8eaee5e535ea3a10721 # v6.4.1
    strategy:
      matrix:
        version:
          - v1
          - v2
    with:
      schema-command: 'spectacular --validate --api-version ${{ matrix.version }}'
      schema-path: 'src/objects/api/${{ matrix.version }}/openapi.yaml'
      python-version: '3.11'
      django-settings-module: 'objects.conf.ci'
      apt-packages: 'libgdal-dev gdal-bin'
